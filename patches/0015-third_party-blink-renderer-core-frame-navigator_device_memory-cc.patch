diff --git a/third_party/blink/renderer/core/frame/navigator_device_memory.cc b/third_party/blink/renderer/core/frame/navigator_device_memory.cc
index 05fa33e456..666154ddaa 100644
--- a/third_party/blink/renderer/core/frame/navigator_device_memory.cc
+++ b/third_party/blink/renderer/core/frame/navigator_device_memory.cc
@@ -9,9 +9,36 @@
 #include "third_party/blink/renderer/core/dom/document.h"
 #include "third_party/blink/renderer/core/frame/local_dom_window.h"
 
+#include <cmath>  // UXR
+#include <cstdlib>  // UXR
+#include "base/command_line.h"  // UXR
+#include "base/strings/string_number_conversions.h"  // UXR
+#include "base/uxr_config.h"
+
 namespace blink {
 
 float NavigatorDeviceMemory::deviceMemory() const {
+  // UXR Layer 2: persona-driven via renderer cmdline switch.
+  double v = 0.0;
+  // UXR P1-A: StringToDouble + snap to the web-exposed buckets {0.25..8}.
+  if (base::StringToDouble(
+          base::UxrConfig::GetInstance().Get("uxr-device-memory"), &v) &&
+      v > 0.0) {
+    const double kB[] = {0.25, 0.5, 1.0, 2.0, 4.0, 8.0};
+    double best = kB[0];
+    for (double b : kB) {
+      if (std::abs(b - v) < std::abs(best - v))
+        best = b;
+    }
+    return static_cast<float>(best);
+  }
+  // Synthetic memory buckets; an absent seed preserves the host value.
+  {
+    const float ph_pool =
+        base::UxrConfig::GetInstance().GetSeededDeviceMemory();
+    if (ph_pool > 0.0f)
+      return ph_pool;
+  }
   return ApproximatedDeviceMemory::GetApproximatedDeviceMemory();
 }
 
